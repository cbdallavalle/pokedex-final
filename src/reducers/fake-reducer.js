// const fakeReducer = (state='this is a fake action', action) => {
//   switch (action.type) {
//   default:
//     return state;
//   }
// };

// export default fakeReducer


export const storeTypes = (state={}, action) => ({ 
  switch (action.type) {
    case 'UPLOAD_TYPES':
      return state
    default:
      return state
  }
})
